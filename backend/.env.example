SECRET_KEY=your-secret-key-here
DEBUG=True

# MySQL is the intended database for this project. Set DB_ENGINE=mysql or provide DB_HOST.
# If no MySQL config is given, it falls back to SQLite for local development.
DB_ENGINE=mysql
DB_NAME=herbacam
DB_USER=root
DB_PASSWORD=your-mysql-password
DB_HOST=127.0.0.1
DB_PORT=3306

# Alternative single-line config:
# DATABASE_URL=mysql://root:your-mysql-password@127.0.0.1:3306/herbacam

OPENROUTER_API_KEY=your-openrouter-key
OPENROUTER_MODEL=google/gemini-2.0-flash-exp:free
